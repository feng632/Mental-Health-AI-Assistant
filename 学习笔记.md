# 学习笔记

> 项目：ai-vue（Vue 3 + Vite + Element Plus + Pinia + Vue Router）

---

## 2026-08-20（登录功能）

> 主题：**打通登录功能** + 解决「网站打不开」和「登录 403」两个 bug

### 一、写了什么

**1. 登录页 login.vue**

- 表单 UI：`el-form` + `el-form-item` + `el-input`，用 `ref="ruleFormRef"` 拿到表单实例
- 表单数据：`formData`（`reactive`），含 `username` / `password`
- 表单校验规则：`rules`，用户名和密码都必填，`trigger: 'blur'`
- 登录逻辑 `submitForm`：
  - 先 `formEl.validate()` 校验表单
  - 校验通过后调 `login(formData)` 请求后端
  - 成功把 `token` 和 `userInfo` 存入 localStorage

**2. 接口封装 api/admin.js**

```js
import service from "@/utils/request";

export function login(data) {
    return service.post('/user/login', data);
}
```

**3. axios 封装 utils/request.js**

- 创建 axios 实例：`baseURL: '/api'`，超时 5s
- **请求拦截器**：每次发请求前自动从 localStorage 取出 token，塞进请求头
- **响应拦截器**：解包响应外壳 `{code, data, msg}`，处理业务码（成功 / 登录过期 / 其他错误）

**4. vite.config.js 开发代理**

```js
server: {
    host: '127.0.0.1',
    port: 5173,
    proxy: {
        '/api': {
            target: 'http://159.75.169.224:1235',
            changeOrigin: true
        }
    }
}
```

作用：前端请求 `/api/xxx` 时代理转发到真实后端，避免跨域。

### 二、弄懂的知识点

**1. Element Plus 表单校验：`formEl.validate` 是哪来的？**

`ref="ruleFormRef"` 拿到的是 **Element Plus 的 ElForm 组件实例**，`validate` 是它在内部用 `expose()` 明确暴露的方法。同类方法还有 `validateField` / `resetFields` / `clearValidate`。

回调签名是 `(valid, fields)`：`valid` = 是否全部通过，`fields` = 未通过的字段集合。

⚠️ `console.log(fields)` 会报错——`fields` 是回调的**第二个参数**，只能回调里用。校验通过拿数据，直接读 `formData`。

触发时机：`trigger: 'blur'` 失焦时、`trigger: 'change'` 值变化时、调 `validate()` 手动强制。

还支持 Promise 写法（二选一，别混用）：`formEl.validate().then(...).catch(...)`。

**2. token 认证的完整闭环（前后端如何配合）**

```
① 登录成功 → 后端返回 token
② 前端把 token 存进 localStorage
③ 每次请求 → 请求拦截器自动把 token 加进请求头
④ 后端校验 token → 过期则返回 code='-1'
⑤ 响应拦截器发现过期 → 弹提示、清空本地、跳回登录页
```

- `token` 是后端签发的身份凭证（身份证）
- `userInfo` 是对象，localStorage 只能存字符串，要 `JSON.stringify`
- 拦截器 = 一次性给所有请求带 token，不用每个请求手动写

**3. Promise 和 async/await：`.then(data => ...)` 里的 data 哪来的？**

- 请求是**异步**的，结果要"晚点"才能拿到
- Promise 是装异步结果的**盒子**：pending（进行中）→ fulfilled（成功）/ rejected（失败），不可逆
- `.then(回调)`：等盒子填好，把结果传给回调第一个参数，参数名随意
- `const data = await login(...)` ≡ `.then(data => ...)`，await 必须在 async 函数里
- **`.then` 只在成功时执行**；失败走 `.catch`，不补会变 unhandled rejection

**4. 响应拦截器做了「解包」**

后端返回 `{code, data, msg}`，拦截器按业务码分派：`code==='200'` → 只返回内层 `data`；`code==='-1'` → 登录过期处理；否则 → 弹提示并 reject。

**核心认知**：`code` 是**业务码**，不是 HTTP 状态码。HTTP 200 只代表网络层通了。你们后端登录失败返回 `code:"BUSINESS_ERROR"`，会走到 else → reject，所以**登录失败不进 `.then`**。

**5. axios 拦截器分类**

| 类型 | 时机 | 典型用途 |
|------|------|---------|
| 请求拦截器 `request.use` | 发请求前 | 加 token、公共参数 |
| 响应拦截器 `response.use` | 收到响应后 | 解包、错误处理、登录过期跳转 |

**6. vite.config.js 的两个底层机制**

- JS 对象同名 key 后面覆盖前面：写两个 `server`，第一个整个被丢
- 代理路径拼接：`target 路径 + 原始完整路径`，**不剥离匹配前缀** → target 带 `/api` 就会拼成双 `/api`

### 三、解决的 Bug

**Bug 1：网站打不开（访问 127.0.0.1:5173 连不上）**

症状：dev server 起了，浏览器访问 `127.0.0.1:5173` 连不上。

排查：`netstat -ano | grep 5173` 看到监听地址是 `[::1]:5173`（**IPv6**），不是 `127.0.0.1`（IPv4）；curl 三个地址确认。

根因：vite.config.js 写了**两个 `server` 键**，第二个把第一个覆盖了，`host: '127.0.0.1'` 失效 → vite 默认绑到 IPv6 `::1` → IPv4 连不上。

修复：两个 `server` 合并成一个。

同类问题：连不上先看监听地址是不是 `[::1]`；配置里出现重复 key 一定是覆盖 bug。

**Bug 2：登录请求返回 403，不是 200**

症状：直连后端 `/api/user/login` 返回 200，走 vite 代理返回 403 空 body。

排查（对比法 + 日志）：
1. 直连后端 = 200（业务错误 JSON）→ 后端通
2. 走代理 = 403 → 问题在代理
3. 排除 CSRF（GET 也 403）、排除请求头 → 是代理转发那一跳
4. proxy 加 `configure` 钩子打印转发日志 → `path=/api/api/user/login` **双 /api**

根因：vite 代理拼接规则 `target + 原始路径` 且不剥前缀，target 带了 `/api` → 双 `/api` → Spring Security 403。

修复：`target` 去掉 `/api`。

方法论：直连 vs 代理对比定位环节；加日志看真实数据；看响应头（`x-frame-options`）判断是谁拒的。

### 四、待办

- [ ] 登录失败补 `.catch(error => ElMessage.error(error))`
- [ ] 注册页 register.vue 接后端
- [ ] vite `__dirname` 警告 → 改成 `import.meta.dirname`
- [ ] 加路由守卫：未登录（无 token）跳登录页

---

## 2026-08-21（知识文章页开发）

> 主题：**知识文章页开发** —— 文章列表表格、分页、搜索、新增弹窗
> 核心：Vue 组件通信（props / emit / v-model）+ 数组遍历方法 + 生命周期钩子

### 一、写了什么

**1. 知识文章列表页 knowledge.vue**

- `el-table` 文章表格：标题 / 分类 / 作者 / 阅读量 / 发布时间 / 操作列
- `el-pagination` 分页（`prev,pager,next`）
- 搜索表单：复用 `TableSearch` 组件（由 `formItem` 配置驱动渲染）
- 接口：`articlePage()` 分页查询 + `categoryTree()` 分类树

**2. 弹窗组件 ArticleDialog.vue**

- 新增文章弹窗，用 `v-model` 控制开/关
- 完整走了一遍 **props + emit + computed 桥** 的双向绑定

### 二、弄懂的核心知识点

**1. 生命周期钩子 onMounted**

"组件挂载完成那一刻自动执行一次"，用来发初始请求：
```js
onMounted(async () => {
    const data = await categoryTree()
    ...
})
```
区别：普通顶层代码在组件渲染完前就执行，`onMounted` 保证 DOM 已就位。

**2. map / forEach / filter 三兄弟**

| 方法 | 返回值 | 用在哪 |
|------|--------|--------|
| `map` | 加工后的**新数组** | 要结果（如把分类数据转成下拉选项） |
| `forEach` | 无 | 只要遍历干点事 |
| `filter` | 筛出的数组 | 筛选 |

口诀：**要结果用 map，只要过程用 forEach**。用 `map` 却丢弃返回值，等于白跑。

**3. 函数引用 vs 函数调用（漏括号的坑）**

```js
await categoryTree     // ❌ 拿到的是函数本身，没发请求
await categoryTree()   // ✅ 真正执行
```
漏括号**不报错、不发请求**，拿到的是函数体——静默 bug，很难发现。

**4. ref 的 `.value` 与两个"value"撞名**

- `categories.value` 的 `.value` —— ref 取值的**语法**（开盒子的钥匙）
- 选项对象的 `value: item.id` —— 业务数据（分类 id）
- **模板里 ref 自动解包**（写 `dialogVisible` 不用 `.value`）；**script 里必须 `.value`**

**5. 下拉选项 label/value 约定**

`<el-option>` 用 `:label`（显示名字）、`:value`（提交 id）。字段名必须叫 `label`/`value`，Element Plus 才认。名字存在 `label` 里，不是丢了。

**6. props / emit / 单向数据流**

```
父 ──props──▶ 子   （只读，不能改）
父 ◀──emit─── 子   （发事件"请求"父组件改）
```
- **props**：父传子，子组件**永远不能直接改 props**
- **emit**：子传父，喊话让父组件自己改
- 数据流单向：子要改父，只能 emit 请求

**7. v-model 语法糖 + computed 桥（弹窗开关）**

`v-model:modelValue="x"` 等价于 `:modelValue="x" @update:modelValue="val => x = val"`。

子组件用**可写 computed** 做桥：
```js
const dialogVisible = computed({
    get: () => props.modelValue,                  // 读：父传的开关
    set: (val) => emit('update:modelValue', val)  // 写：喊话请父改
})
```

流程：
- 打开 = 父主动推（4 步）：父改 `dialogVisible` → props 传下 → getter 读 → 显示
- 关闭 = 子请求→父改→传回（8 步）：el-dialog 写入 → setter → emit → 父改 → 重渲染 → props 传回 → 关闭

约定：事件名必须叫 `update:modelValue`。`@close` 是关闭后的回调，**不负责关闭**。

**8. el-pagination 的 layout 标记**

合法标记：`prev` / `pager` / `next` / `sizes` / `total` / `jumper`。
- 页码数字是 **`pager`**，不是 `page`（写 `page` 被忽略，只剩箭头）
- `pager` 只在 `total > 0` 时才显示

### 三、解决的 Bug

**Bug 1：模板属性少了 `=` → 整页编译失败白屏**

```html
:page-size:"pagination.size"   <!-- ❌ 少了 = -->
:page-size="pagination.size"   <!-- ✅ -->
```
少了 `=`，Vue 编译器把整串当属性名解析，报 `Unexpected token, expected ","`，整个页面编译失败。Vite 错误里的 `"line":43` 精确定位。

**Bug 2：import 路径 `@./` 写错**

```js
import ArticleDialog from '@./components/ArticleDialog.vue'  // ❌
import ArticleDialog from '@/components/ArticleDialog.vue'   // ✅
```
`@` 是 `src` 别名，`@./` 既不是别名也不是相对路径，Vite 解析不了。

**Bug 3：弹窗里 `dialogVisible` 未定义**

用了 `v-model="dialogVisible"` 但 script 里没声明，点关闭 `dialogVisible.value=false` → `ReferenceError`，弹窗关不掉。正确做法见知识点 7 的 computed 桥。

**Bug 4：`conputed` 拼错 + 忘导 computed + emit 未声明**

- `conputed` → `computed`（拼写）
- `import { computed } from 'vue'`（忘导入）
- `const emit = defineEmits(['update:modelValue'])`（emit 没声明就调用）

任一不修，一运行就 `xxx is not defined`。

**Bug 5：`totlal` 拼写错误**

```js
const {records,totlal} = await articlePage(params)  // ❌
```
`totlal` → `total`，否则 `pagination.total` 永远是 undefined。

**Bug 6：el-pagination 的事件名和标记**

- `@change` → `@current-change`（翻页事件叫 current-change）
- layout `page` → `pager`（页码数字的标记）

**Bug 7：分页接口 403（不是代码 bug）**

`/api/knowledge/article/page` 需要登录 token，未登录 403 → 表格空、pager 不显示数字。受保护接口调试时记得先登录。

### 四、待办

- [ ] 登录后验证分页返回字段名 `records` / `total` 是否与后端一致
- [ ] 状态按钮 `v-if="status === 0"` 用数字，但下拉选项值是字符串 `'0'`，确认后端返回类型
- [ ] 编辑 / 发布 / 下线 / 删除按钮功能（目前是空按钮）
- [ ] 装 AI 联想插件（推荐通义灵码），装完只保留一个内联补全插件
- [ ] （编辑器）`quickSuggestions` 已恢复 `on`；AI 灰字联想需靠专门插件

---

### 补充：封面图片上传（同日）

> 在文章弹窗里加了封面上传 + 标签多选。核心：`el-select` 组合属性、flex 居中前提、MIME 类型、FormData 文件上传、以及一次完整的"前端已就绪但后端 500"的定位。

#### 一、写了什么

- `el-upload` 封面上传：`before-upload` 校验 + `http-request` 自定义上传
- `api/admin.js` 加 `uploadFile`：用 `FormData` 以 multipart 方式 POST `/file/upload`
- 标签多选下拉：`el-select` + `multiple filterable allow-create`

#### 二、知识点

**1. el-select 三属性组合**

- `multiple`：多选，`v-model` 值从单个变**数组**
- `filterable`：可输入搜索过滤选项
- `allow-create`：输入不存在的值可**创建新选项**（前提：必须先有 `filterable`）
- 三者组合 = 标签输入框（能选预设、能造新标签）

**2. flex 居中不生效，先查 `display:flex`**

`justify-content`（水平）、`align-items`（垂直）是 **flex 专属属性**，容器不写 `display:flex` 就全部失效。居中不生效的第一个排查点。

**3. MIME 类型（file.type）**

- jpg 文件的 `file.type` 是 `image/jpeg`，**不是** `jpg`
- `file.type.startsWith('jpg')` 永远 false（因为以 `image/` 开头）
- 判断图片：`startsWith('image/')`（所有图片）或 `=== 'image/jpeg'`（严格 JPG）

**4. FormData + multipart 文件上传**

- 文件是二进制，不能放 JSON，必须用 `FormData`
- `formData.append('字段名', 值)` 装字段
- `Content-Type: multipart/form-data` **让 axios 自动设置**（传 FormData 时它会加 boundary），手写反而可能出问题

**5. 上传成功后的两条路**

```js
imgUrl.value = `${fileBaseUrl}/${fileRes.filePath}`   // 完整 URL → 弹窗预览
formData.coverImage = fileRes.filePath                // 纯路径 → 提交文章时带给后端
```

一个给"眼前显示"，一个给"最终提交"。字段名（`filePath`/`url`/`path`）取决于后端返回，要对上。

#### 三、解决的问题 / 定位的问题

**① `startsWith('jpg')` 永远拦图片**

`file.type` 是 `image/jpeg`，以 `image/` 开头，`startsWith('jpg')` 恒为 false → 传什么图都被拦，提示"只能是JPG"。改回 `startsWith('image/')` 或 `=== 'image/jpeg'`。

**② `crypto.randomUUID()` 误加 `.value`**

`randomUUID()` 返回普通字符串，不是 ref，`businessId.value` 是 undefined。只有 Vue 的 `ref` 才需要 `.value`——用多了会条件反射，但原生 API 返回字符串就直接用。

**③ 后端 /file/upload 返回 `code:500 系统错误`（非前端问题）**

完整定位过程（这套方法论值得记住）：
1. 前端校验、参数、字段名全部核对正确
2. 走 vite 代理 vs **直连后端**（`http://159.75.169.224:1235`）→ 结果一样，排除代理
3. 对比其他接口：`/user/login` 正常返回业务错误、`/knowledge/category/tree` 返回 403（需登录）→ **后端整体活着**
4. 唯独 `/file/upload` 返回统一"系统错误" → **后端上传模块内部异常**（大概率存储服务未配置，如 OSS/MinIO/本地目录）

结论：前端已就绪，问题在后端存储环节，**等自己实现后端时再查**（到那时能看到自己的存储逻辑和全局异常处理，问题从黑盒变白盒）。

#### 四、待办

- [ ] 后端修好 `/file/upload` 后，确认返回字段名（`filePath` / `url` / `path`），对上 `formData.coverImage`
- [ ] 上传成功后验证：`imgUrl` 预览 + `formData.coverImage` 存值
- [ ] `beforeUpload` 文案与判断对齐（如果想严格限 JPG，accept 也要改成 `.jpg,.jpeg`）

---

## 2026-08-22（富文本编辑器 + 封面上传全链路打通）

> 主题：给文章页加**富文本编辑器**（wangeditor 5），并解决封面上传的完整链路——字段名拼写 + 图片 URL 拼接两个坑。

### 一、写了什么

1. **安装 wangeditor 5（Vue3 版）**
   - `@wangeditor/editor@5.1.23` + `@wangeditor/editor-for-vue@5.1.12`
   - ⚠️ `@wangeditor/editor-for-vue@1.x` 只支持 **Vue 2**；5.x 才支持 Vue 3
2. **新建 RichTextEditor.vue**
   - 富文本编辑器组件：工具栏（字号/字体/颜色/列表/链接/撤销重做）+ 内容区 + 字数统计条
   - `v-model` 双向绑定 + `setHtml` 回显 + 自定义面板样式
3. **ArticleDialog 集成**：文章内容改用富文本编辑器、标签多选（multiple+filterable+allow-create）、提交逻辑
4. **封面上传打通**：`uploadFile` 字段名修正 + `fileBaseUrl` 修正

### 二、弄懂的知识点

**1. ERESOLVE / peer 依赖冲突**

npm 装包时检查 `peerDependencies`（同伴依赖）——包声明要求 Vue 2，项目是 Vue 3，npm 直接拒绝：
```
peer vue@"^2.6.14" from @wangeditor/editor-for-vue@1.0.2
```
**别用 `--force` 硬装**（跳过了检查但运行时崩）。正确做法：找支持 Vue 3 的版本（5.x）。

**2. MIME 类型（file.type）**

jpg 的 `file.type` 是 `image/jpeg`，**不是** `jpg`。`startsWith('jpg')` 永远 false，会拦截所有图片。判断图片用 `startsWith('image/')`（所有图片）或 `=== 'image/jpeg'`（严格 JPG）。

**3. nextTick —— 等 DOM 更新完再干活**

```js
nextTick(() => { editor.setHtml(formData.content) })
```
编辑器刚创建时内部 DOM 可能没就绪，直接 setHtml 可能失败。`nextTick` 等 Vue 完成本轮 DOM 更新再执行。**参数是回调函数**，写成 `nextTick({...})` 是语法错误。

**4. 回调参数由调用方传入**

`handleEditorCreated(editor)` 的 `editor` 是 wangeditor 在创建完成后调用回调时**塞进来的**——和 `.then(data)` 的 `data` 一个道理：谁调用回调，谁传参数。

**5. 自定义组件的 v-model**

`v-model:modelValue="x"` = `:modelValue="x"` + `@update:modelValue="val => x = val"`。子组件用 computed 的 get/set 做桥。富文本编辑器也是这么接内容的。

**6. defineEmits 声明事件，名字要前后一致**

```js
const emit = defineEmits(['update:modelValue', 'success'])  // 声明
emit('success')                                            // 发
// 父组件必须 @success="..." 才能收到
```
今天踩了：子组件发 `success`，父组件监听 `@submit`，对不上 → 回调永不触发。

**7. 对象浅拷贝**

`{...formData}` 拷贝引用。`delete submitData.tagArray` / `tags: join(...)` 都只影响新对象，**不影响原 formData**。打印时注意打印对对象（打印 formData 会看到 tagArray 还在、tags 空，其实提交的 submitData 是正确的）。

**8. 对象属性名引号可选**

合法标识符（`title`/`tagArray` 等）可加可不加；含空格/连字符（`first name`/`my-key`）才必须加。定义时可带引号，**访问时不能**（`formData.tagArray` 用点号）。

**9. 前后端字段名一致性（最重要的教训）**

字段名拼写错（`businesss*` 三个 s vs `business*` 两个 s）→ 后端收不到正确字段 → 返回**笼统的"系统错误"**。排查时要对照后端接口文档/实体类核对字段名。

**10. 静态文件不走 /api**

接口请求走 `/api`（vite 代理转发），但**图片等静态资源挂在域名根路径**。`fileBaseUrl` 拼接图片地址时不能带 `/api`，否则拼出 `/api/files/...` → 403。

### 三、解决的 Bug

**Bug 1：ERESOLVE 装不上 wangeditor**

`editor-for-vue@1.0.2` 是 Vue2 的 → 换成 5.x（peer vue ^3.0.5）解决。

**Bug 2：`@components/...` import 路径错**

```js
import RichTextEditor from '@components/...'   // ❌
import RichTextEditor from '@/components/...'  // ✅
```
`@` 别名后必须跟 `/`，`@components` 会解析成不存在的 `srccomponents`。报 `Failed to resolve import`。

**Bug 3：`console,log` 逗号写错**

```js
console,log(formData)   // ❌ 逗号表达式 → log is not defined
console.log(formData)   // ✅
```
`console, log(...)` 把 `log` 当独立变量 → ReferenceError。

**Bug 4：`formData.tagArray` 未初始化**

模板 v-model 用到但 formData 里没有 → `undefined.join(',')` 会崩。multiple 模式需要数组，初始化 `tagArray: []`。

**Bug 5：`nextTick({...})` 参数错**

传了对象字面量（还是语法错误），应为 `nextTick(() => {...})`。

**Bug 6：事件名对不上**

子组件 `emit('success')`，父组件 `@submit` → 永不触发。统一成 `@success`。

**Bug 7：`startsWith('jpg')` 永远拦图片**

MIME 是 `image/jpeg`，改 `startsWith('image/')`。

**Bug 8（本次最大）：后端 `/file/upload` 返回"系统错误"**

表象：上传报"系统错误"/"网络请求失败..."。
排查过程：
1. 前端参数、逻辑核对正确
2. 直连 vs 代理 → 都 500，排除代理
3. 对比接口：login 正常 → 唯独 upload 挂
4. 一度误判为"后端存储问题"

**真正根因（用户点破）**：前端字段名 `businesss*`（三个 s）拼错，后端收不到 `businessType/businessId/businessField` → 抛异常 → 统一"系统错误"。字段名改对后返回 `code:200`。

排查教训：**curl 测试时用了和前端一样的错字段名**（`businesssType`），导致一直复现 500、误导方向。测接口要用**后端约定的正确字段名**。

**Bug 9：上传成功但封面不显示（403）**

上传通了但图片加载失败。根因：`fileBaseUrl` 带 `/api`，拼出 `/api/files/...` → 403。去掉 `/api` → `http://159.75.169.224:1235/files/...` → 200 ✅。

### 四、待办

- [ ] 文章提交（createArticle）完整流程验证（含 tags 逗号拼接、封面、富文本内容）
- [ ] 文章列表回显封面色（如果有）
- [ ] 富文本内容在文章详情/列表的回显
- [ ] `beforeUpload` 文案与判断对齐（严格限 JPG 可选）
- [ ] 装 AI 联想插件（推荐通义灵码），装完只保留一个内联补全插件

---

## 2026-08-28（列表/详情/弹窗/分页 + 咨询、情绪两个页面）

> 主题：完善**知识文章页**（编辑/发布/提交），并新建**咨询管理**、**情绪日志**两个页面。核心知识：`el-table` 插槽、分页机制、`v-model` 控制弹窗、`TableSearch` 复用组件；以及反复踩的"**名字前后不一致**"错误。

### 一、写了什么

1. **知识文章页**：编辑按钮（查详情回显）、发布按钮（确认框 + 改状态 + 刷新）、文章提交（新增/编辑分流）
2. **咨询管理页 consultations.vue**：会话列表、详情弹窗（消息记录）
3. **情绪日志页 emotional.vue**：列表 + 搜索表单（TableSearch）+ 详情弹窗

### 二、弄懂的核心知识点

**1. 回调/插槽参数由调用方传入（本期反复验证的规律）**

| 场景 | 参数 | 谁传的 |
|------|------|--------|
| `.then(data)` | data | Promise |
| `handleEditorCreated(editor)` | editor | wangeditor |
| `@search="handleSearch"` 的 formData | 搜索条件 | TableSearch（点查询时 emit）|
| `#default="scope"` → `scope.row` | 当前行数据 | el-table 渲染每行时 |

**规律**：你只管在签名里声明变量名，调用方保证带着数据来。

**2. el-table 列：`prop` vs 插槽**

- `prop="字段名"`：一列直接显示一个字段（el-table 内部自动取 `scope.row.字段`）
- `<template #default="scope">`：自定义单元格（组合多个字段 / 放按钮 / 条件显示）
- 口诀：**prop 用于"拿来就显示"，插槽用于"要加工/组合/放组件"**

**3. 分页机制（完整一圈）**

```
前端带 currentPage + pageSize 请求
  → 后端 SQL：LIMIT pageSize OFFSET (currentPage-1)*pageSize
  → 返回 { records: 这一页的数据, total: 总条数 }
  → 前端：tableData = records（渲染），pagination.total = total
  → el-pagination：页数 = ceil(total / pageSize)，显示 prev/pager/next
  → 点页码 → @current-change → 改 currentPage → 重新请求 → 循环
```

关键：**OFFSET = (currentPage-1) * pageSize**；`el-pagination` 用 `total` 和 `page-size` 算页码数。

**4. 弹窗开关：同组件 vs 跨组件**

- **同组件**（emotional/consultations）：`const dialogVisible = ref(false)`，点按钮设 `true` 打开，`v-model` 会在用户点 X 时自动写回 `false` 关闭——**一个 ref 就够**，不用管关闭
- **跨组件**（ArticleDialog）：开关在父组件 → 才需要 props + emit + computed 桥

**5. ElMessageBox.confirm 三参数**

```js
ElMessageBox.confirm(消息, 标题, { confirmButtonText, cancelButtonText, type })
```
点确认走 `.then`，取消走 `.catch`。⚠️ 消息里要插变量必须用**反引号**（`` `...${x}...` ``），单引号不插值。

**6. SCSS 嵌套依赖父选择器**

`.messages-container { .messages-list { .message-header {...} } }` 编译成完整后代选择器。**最外层父类名写错，里面所有嵌套样式全部失效**（楼找不到，楼里的房间也找不到）。

**7. TableSearch 复用组件**

传 `formItem` 配置数组 → 自动渲染搜索表单 → 点查询 `emit('search', formData)` → 父组件 `handleSearch` 接收条件。配置项：`component`（input/select）、`label`、`prop`、`placeholder`、`options`。

### 三、解决的 Bug（本期几乎全是"名字不一致"）

**Bug 1：`updateArticle(submitData)` 少传 id → loading 卡住转圈**

admin.js 定义 `updateArticle(id, data)`，调用只传了一个 → URL 变成 `[object Object]`、body 空 → 请求失败 → `.then` 不执行 → `loading` 卡 true。改 `updateArticle(submitData.id, submitData)`。

**Bug 2：`sessionDetail.value = ros` 拼写错误**

`ros` → `res`。ReferenceError 卡死回调，后面的 `showDetailDialog.value = true` 不执行 → 弹窗不出现。

**Bug 3：`sessionId` 参数名不一致**

```js
function getConsultationDetail(id) {
    service.get(`/sessions/${sessionId}/messages`)  // 参数是 id，用的是 sessionId
}
```
声明 `id`、引用 `sessionId` → ReferenceError。函数参数名和函数体引用必须一致。

**Bug 4：v-for 变量名不一致**

```html
v-for="sessionMessage in sessionMessages" ... {{ message.id }}
```
v-for 声明 `sessionMessage`，模板用 `message` → undefined.id 报错。**v-for 声明什么就用什么**。

**Bug 5：SCSS 父容器类名不一致**

模板 `message-container`（无 s），样式 `.messages-container`（有 s）→ 最外层匹配不上 → `.message-header` 等所有嵌套样式全失效。统一类名。

**Bug 6：`comp` vs `component`**

formItem 配置写 `comp`，TableSearch 读 `item.component` → 渲染不出输入框。配置字段名要和组件读取的一致。

**Bug 7：`getEmotionaPage` vs `getEmotionalPage`（漏 l）**

import 名和 admin.js 导出名不一致 → `does not provide an export named` → 页面打不开。ES module 对名字零容忍。

**Bug 8：`pagination` ref 忘开盒（consultations）**

`pagination.currentPage = x` 改的是 ref 盒子 → `pagination.value.currentPage` 永远是 1 → 翻页总是请求第 1 页 → "分页像写死"。改 `pagination.value.currentPage`。

**Bug 9：`res.records` 多一层**

```js
const {records,total} = res.records  // records 是数组，解构出 undefined
```
应直接 `const {records,total} = res`。

**Bug 10：`:page-size` 字段名**

`:page-size="pagination.size"`，但 pagination 里字段叫 `pageSize` → undefined。字段名要对上。

**Bug 11：`handlePublish` 单引号不插值**

`'确认发布${row.title}吗'`（单引号）→ `${row.title}` 原样显示。要用反引号。

**经验总结（本期最大收获）**：以上 11 个 bug 大多是**声明一个名字、用的时候写成另一个**——`ros`/`res`、`sessionId`/`id`、`message`/`sessionMessage`、`comp`/`component`、`getEmotionaPage`/`getEmotionalPage`、`message-container`/`messages-container`。**JS 只认你声明过的名字**，排查时对比"声明处"和"使用处"。

### 四、待办

- [ ] 咨询/情绪页的详情弹窗内容填充（currentDetail 存了但还没显示）
- [ ] 情绪页分页的 total 字段名/参数名与后端核对
- [ ] 文章详情回显 + 富文本编辑器在编辑时的清空（watch else + RichTextEditor 内部 setHtml）
- [ ] 删除按钮功能（改状态或删除接口）
- [ ] 装 AI 联想插件（推荐通义灵码），装完只保留一个内联补全插件

---

## 2026-08-29（仪表盘统计页 + 路由守卫 + 前端站点雏形）

> 主题：修复**分析统计（Dashboard）页**多次打不开、打通**路由守卫**（按角色跳转）、搭建**前端站点**雏形（布局 + 首页 + 注册/登录闭环）。核心知识：模板渲染崩溃的两大典型原因（没导入 / 访问 undefined）、CSS 优先级与行内样式、`?.` / `??` 安全访问、路由守卫的 next() 契约，以及一堆"名字拼写不一致"导致页面打不开的报错。

### 一、写了什么

1. **dashboard.vue 分析统计页**：4 张统计卡片（总用户数 / 情绪日志 / 咨询会话 / 平均情绪）+ ECharts 情绪趋势分析占位（`echarts.init` 骨架，数据未接）
2. **前端站点雏形**：`FrontendLayout` 布局（导航栏 + 首页/登录/注册入口 + 退出登录）、`home.vue` 首页（Hero 区 + 幽灵按钮）
3. **路由调整**：删掉重建 `frontendRoutes`；路由守卫补普通用户（userType=1）跳转逻辑（访问 `/back`、`/auth` 强制回首页）
4. **register.vue 注册页修复**：点击无反应 → 参数名 / ref 开盒 / 校验回调全链路
5. **login.vue**：理清"用户跳转由守卫兜底"的分工

### 二、弄懂的核心知识点

**1. 模板渲染崩溃的两大典型原因**

- **用了但没导入**：script 里 `ref({})`，但 `import { onMounted, reactive }` 没导 `ref` → `ref is not defined` → 页面白屏
- **访问 undefined 的属性**：数据初始 `{}`，模板写 `aiData.systemOverview.totalUsers` → 首次渲染 `systemOverview` 是 undefined → TypeError

解法：**可选链 `?.` + 空值合并 `??`**，数据没回来时安全兜底：

```js
{{ aiData.systemOverview?.totalUsers ?? 0 }}
```

`?.` 遇 undefined 短路返回 undefined，`??` 再用默认值 0 兜底。这是渲染接口数据的标配写法。

**2. CSS 优先级：行内样式 > hover 伪类（为什么悬停没反馈）**

优先级从高到低：`!important` > **行内 style** > id > class / 伪类 > 标签。

把 `background` / `color` 写进行内 style 后，`.el-button:hover` 的伪类样式（优先级 0,2,0）永远压不过行内（1,0,0,0）→ **悬停时按钮毫无变化**。解法：样式移到 class 里，并显式补 `&:hover` 自定义反馈。

**3. `color: transparent` 不是"透明背景"**

`color` 控制的是**文字颜色**。文字变透明后会露出**按钮自己的白色背景**，看起来像"白字没生效"。想要幽灵按钮（透明底 + 白字 + 描边），应该：

```scss
.ghost-btn {
    background: transparent;
    color: white;
    border-color: aliceblue;
    &:hover { background: white; color: #3d8a7a; }
}
```

**4. 路由守卫的 next() 契约**

- 守卫里**必须调用一次 `next()`**，不调用 → 导航永久挂起 → 页面打不开（不报错，最难查）
- 分支要兜底：条件都不满足时加 `else { next() }`
- **严格相等陷阱**：后端返回 `userType: "2"`（字符串），`"2" === 2` 为 false → 两个 if 都不进 → next() 没被调。统一 `Number(x)` 再比较（和登录页 `Number(data.userInfo.userType)` 保持一致）

**5. ES Module 对"名字"零容忍（Vite 编译期直接报错）**

| 写错 | 正确 | 报错形式 |
|------|------|---------|
| `ELMessage` | `ElMessage` | `"ELMessage" is not exported by element-plus` |
| `userRouter` | `useRouter` | `"userRouter" is not exported by vue-router` |
| `@/views/frontend/home.vue` | 存在的文件 | `Failed to resolve import` |
| `hom.vue`（少 e） | `home.vue` | 同上 |

**6. 懒加载路由的报错时机**

`() => import(...)` 的组件**访问到对应路径时才加载**。所以这类"缺导入/缺文件"的报错只在进入某个页面时炸——登录页、注册页、前端首页都可能各自独立翻车，排查时先看当前 URL 对应的文件。

**7. 模板 ref 与组件公开方法（复习 + 深化）**

`ref="submitFormRef"` 拿到的是 el-form **组件实例**，`validate` 是它用 `expose()` 暴露的公开方法，`submitForm(submitFormRef)` 传入后才能调用。名字必须和模板里 `ref="..."` 完全一致（`ruleFormRef` ≠ `submitFormRef`）。

### 三、解决的 Bug

**Bug 1：dashboard 页面打不开（`ref` 未导入）**
用了 `ref({})` 没从 vue 导入 → `ref is not defined`。补导入解决。

**Bug 2：dashboard 加卡片后又打不开（模板访问 undefined）**
首次渲染 `aiData = {}`，`aiData.systemOverview.totalUsers` 抛 TypeError。改用 `?.` + `??` 兜底（4 处字段）。顺带发现 `src="iconUrl1"` 少了 `:`，只是图不显示、不炸页面。

**Bug 3：登录后路由卡死（userType 字符串 vs 数字）**
后端 `userType` 是字符串 `"2"`，守卫里 `userType === 2` 恒 false，两个分支都不进、`next()` 永不调用 → 导航挂起。修复：`Number(JSON.parse(userInfo).userType)` + 兜底 `else { next() }` + `userInfo` 缺失时清 token 回登录页。

**Bug 4：`Failed to resolve import '@/views/frontend/home.vue'`**
`frontendRoutes` 引用的页面目录不存在（前端站点没开发）→ 整个应用编译失败。处理：删除前端路由（后来开发前端时又加回）。

**Bug 5：加回前端路由后依然打不开（括号不匹配 + 文件名拼错）**
- `children: [` 等三层括号只关了两层 → 语法错误
- 路由引 `home.vue`，实际文件是 `hom.vue`（少个 e，空文件）→ `Failed to resolve import`
- 修复：补齐 `} ] } ]` 闭合；`hom.vue` 正名为 `home.vue`

**Bug 6：注册页点击无反应 + 提交字段错**
- `@click="submitForm(ruleFormRef)"`，模板 ref 叫 `submitFormRef` → `formEl` 是 undefined，`if(!formEl) return` 直接退出
- `register(formData)` 传的是 ref 盒子，应传 `formData.value`
- 校验回调没检查 `valid` → 校验失败也提交。三处一起修。

**Bug 7：注册页打不开（`ELMessage` 拼写）**
element-plus 只导出 `ElMessage`。`ELMessage` 触发 Vite 编译报错。改 `import { ElMessage }`。

**Bug 8：前端布局打不开（`userRouter` 拼写）**
`import { userRouter } from 'vue-router'` → vue-router 没有这个导出。改 `useRouter`。

**Bug 9：幽灵按钮悬停无反馈（行内样式压过 hover）**
`style="background: transparent; color: white"` 行内样式优先级高于 `.el-button:hover` → 悬停不变。样式移到 `.ghost-btn` 类 + 补 `&:hover`。

**经验总结**：这期绝大多数"页面打不开"都指向同一类问题——**名字对不上**：`ref` 忘导、`ELMessage`/`userRouter`/`hom.vue` 拼错、`ruleFormRef`/`submitFormRef` 不一致。Vite 报错信息里的路径和行号都是精确的，照着改即可。另一类是**逻辑级**：守卫没调 `next()`、模板访问 undefined——不报编译错，表现为页面白屏/卡死，需要按"首次渲染时数据是否就绪"来排查。

### 四、待办

- [ ] dashboard 情绪趋势图接入：后端加 `/data-analytics/emotion-trend` 接口 + 前端 `setOption` 渲染（目前只有 init 骨架）
- [ ] dashboard 卡片字段与后端 `/data-analytics/overview` 返回结构核对（确认 `systemOverview` 存在及各字段名）
- [ ] 普通用户登录后自动跳首页：login.vue 的 else 补 `router.push('/')`
- [ ] `home.vue` 首页内容完善（目前是 Hero 区占位）
- [ ] dashboard 卡片图标路径（`iconUrl1`~`iconUrl4`）确认存在
- [ ] 注册页确认密码校验（两次密码一致性）、手机号校验规则
- [ ] 装 AI 联想插件（推荐通义灵码），装完只保留一个内联补全插件


